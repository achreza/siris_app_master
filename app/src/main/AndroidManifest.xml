<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reza.pbobeta">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mainmenu"
            android:label="@string/title_activity_mainmenu" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".barangbaru"
            android:label="@string/title_activity_barangbaru"
            android:theme="@style/AppTheme" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".page_laporan" />
        <activity android:name=".tampilLaporan" />
        <activity android:name=".gantipw"></activity>
    </application>

</manifest>